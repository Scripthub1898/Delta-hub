-- Deobfuscated with MoonSec V3 Deobfuscator Tool
-- Modified: Added auto-invis button that persists after death

local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")

-- Main configuration
local CONFIG = {
    speed = 21,
    jumpPower = 74,
    maxHealth = 185,
    respawnTime = 11,
    debugMode = false
}

-- Invisibility settings
local INVIS_CONFIG = {
    enabled = false,  -- auto-invis state
    normalInvis = false -- manual invis state
}

-- Event connections
local remotes = ReplicatedStorage:WaitForChild("Remotes")
local damageEvent = remotes:WaitForChild("DamageEvent")
local healEvent = remotes:WaitForChild("HealEvent")
local respawnEvent = remotes:WaitForChild("RespawnEvent")

-- Weapon definitions extracted from obfuscated table
local weapons = {
    {
        name = "Sword",
        damage = 15,
        cooldown = 0.8,
        range = 4
    },
    {
        name = "Bow",
        damage = 10,
        cooldown = 1.2,
        range = 30
    },
    {
        name = "Staff",
        damage = 25,
        cooldown = 2.5,
        range = 15
    }
}

-- Invisibility function
local function setInvisible(character, isInvisible)
    if not character then return end
    
    for _, part in ipairs(character:GetDescendants()) do
        if part:IsA("BasePart") then
            part.Transparency = isInvisible and 1 or 0
        elseif part:IsA("Decal") then
            part.Transparency = isInvisible and 1 or 0
        end
    end
    
    local humanoid = character:FindFirstChild("Humanoid")
    if humanoid then
        humanoid.HealthDisplayType = isInvisible and Enum.HumanoidHealthDisplayType.AlwaysOff or Enum.HumanoidHealthDisplayType.AlwaysOn
    end
end

-- Apply invisibility based on current state
local function applyInvisibility(player)
    local character = player.Character
    if not character then return end
    
    if INVIS_CONFIG.normalInvis then
        setInvisible(character, true)
    elseif INVIS_CONFIG.enabled then
        setInvisible(character, true)
    else
        setInvisible(character, false)
    end
end

-- Setup UI Button
local function createUIButton()
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "InvisControl"
    screenGui.Parent = game:GetService("CoreGui")
    
    local autoButton = Instance.new("TextButton")
    autoButton.Name = "AutoInvisButton"
    autoButton.Size = UDim2.new(0, 150, 0, 40)
    autoButton.Position = UDim2.new(0, 10, 0, 10)
    autoButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    autoButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    autoButton.Text = "Auto Invis: OFF"
    autoButton.BorderSizePixel = 0
    autoButton.Parent = screenGui
    
    local normalButton = Instance.new("TextButton")
    normalButton.Name = "NormalInvisButton"
    normalButton.Size = UDim2.new(0, 150, 0, 40)
    normalButton.Position = UDim2.new(0, 10, 0, 55)
    normalButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    normalButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    normalButton.Text = "Invis: OFF"
    normalButton.BorderSizePixel = 0
    normalButton.Parent = screenGui
    
    autoButton.MouseButton1Click:Connect(function()
        INVIS_CONFIG.enabled = not INVIS_CONFIG.enabled
        autoButton.Text = INVIS_CONFIG.enabled and "Auto Invis: ON" or "Auto Invis: OFF"
        
        if INVIS_CONFIG.enabled then
            -- Turn off normal invis when enabling auto
            INVIS_CONFIG.normalInvis = false
            normalButton.Text = "Invis: OFF"
        end
        
        applyInvisibility(Players.LocalPlayer)
    end)
    
    normalButton.MouseButton1Click:Connect(function()
        INVIS_CONFIG.normalInvis = not INVIS_CONFIG.normalInvis
        normalButton.Text = INVIS_CONFIG.normalInvis and "Invis: ON" or "Invis: OFF"
        
        if INVIS_CONFIG.normalInvis then
            -- Turn off auto invis when using manual mode
            INVIS_CONFIG.enabled = false
            autoButton.Text = "Auto Invis: OFF"
        end
        
        applyInvisibility(Players.LocalPlayer)
    end)
    
    return screenGui
end

-- Utility functions
local function calculateDamage(baseDamage, distance, player)
    local character = player.Character
    if not character then return 0 end
    
    local modifier = 1
    
    -- Apply damage falloff based on distance
    if distance > 10 then
        modifier = modifier * (1 - (distance - 10) * 0.02)
    end
    
    -- Apply random variation
    modifier = modifier * (math.random() * 0.2 + 0.9)
    
    return math.floor(baseDamage * modifier)
end

-- Player handling
local function setupPlayer(player)
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild("Humanoid")
    
    humanoid.WalkSpeed = CONFIG.speed
    humanoid.JumpPower = CONFIG.jumpPower
    humanoid.MaxHealth = CONFIG.maxHealth
    humanoid.Health = CONFIG.maxHealth
    
    -- Apply initial invisibility state
    if player == Players.LocalPlayer then
        applyInvisibility(player)
    end
    
    -- Setup damage handling
    damageEvent.OnServerEvent:Connect(function(playerWhoFired, targetPlayer, damageAmount, weaponIndex)
        if playerWhoFired ~= player then return end
        if not targetPlayer or not targetPlayer.Character then return end
        
        local weapon = weapons[weaponIndex or 1]
        if not weapon then return end
        
        local targetHumanoid = targetPlayer.Character:FindFirstChild("Humanoid")
        if targetHumanoid then
            local playerPosition = player.Character.PrimaryPart.Position
            local targetPosition = targetPlayer.Character.PrimaryPart.Position
            local distance = (playerPosition - targetPosition).Magnitude
            
            if distance <= weapon.range then
                local finalDamage = calculateDamage(damageAmount or weapon.damage, distance, targetPlayer)
                targetHumanoid.Health = math.max(0, targetHumanoid.Health - finalDamage)
                
                -- Fire client effects
                remotes.DamageEffect:FireClient(targetPlayer, finalDamage)
            end
        end
    end)
    
    -- Handle respawn to reapply auto-invis
    player.CharacterAdded:Connect(function(newCharacter)
        task.wait(0.5) -- Wait for character to fully load
        if player == Players.LocalPlayer then
            applyInvisibility(player)
        end
    end)
end

-- Create UI for local player only
if game:GetService("Players").LocalPlayer then
    createUIButton()
end

-- Initialize for existing players
for _, player in ipairs(Players:GetPlayers()) do
    setupPlayer(player)
end

-- Setup for new players
Players.PlayerAdded:Connect(setupPlayer)

-- Game loop
RunService.Heartbeat:Connect(function(deltaTime)
    for _, player in ipairs(Players:GetPlayers()) do
        local character = player.Character
        if character and character:FindFirstChild("Humanoid") then
            local humanoid = character:FindFirstChild("Humanoid")
            if humanoid.Health <= 0 then
                if not character:FindFirstChild("Respawning") then
                    local respawning = Instance.new("BoolValue")
                    respawning.Name = "Respawning"
                    respawning.Parent = character
                    
                    task.delay(CONFIG.respawnTime, function()
                        respawnEvent:FireClient(player)
                    end)
                end
            end
        end
    end
end)
